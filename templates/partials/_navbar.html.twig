<nav style="background: #1a2980; color: #fff; padding: 15px 30px; display: flex; align-items: center; justify-content: space-between;">
    <div>
        {# Lien vers la page d'accueil #}
        <a href="{{ path('homepage') }}" style="color: #ffe066; font-weight: bold; font-size: 1.5em; text-decoration: none;">KaraokeTime</a>
    </div>
    <div>
        {# Lien vers la liste des chansons #}
        <a href="{{ path('song_index') }}" style="color: #fff; margin-right: 20px; text-decoration: none;">Chansons</a>
        <a href="{{ path('song_new') }}" style="color: #fff; margin-right: 20px; text-decoration: none;">Ajouter une chanson</a>
        {# Lien vers la page de connexion
        <a href="{{ path('app_login') }}" style="color: #fff; margin-right: 20px; text-decoration: none;">Connexion</a> #}
        {# Lien vers la page d'inscription #}
        {# <a href="{{ path('app_register') }}" style="color: #fff; text-decoration: none;">Inscription</a> #}

    </div>
</nav>